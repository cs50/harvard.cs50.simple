This is CS50 Render.  It exists in two forms:

* **cli**, which offers a command-line interface via which to render source code as a PDF
* **web**, which offers an HTTP API for rendering PDFs and source code as HTML
